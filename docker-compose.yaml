version: '2'
services:
    backend:
        build: ./backend
        volumes:
          - ./backend:/project/src:rw
        working_dir: /project
        ports:
          - "9060:9000"
        expose:
          - "9000"
        restart: unless-stopped
        command: node ./src/index.js
    # frontenddev:
    #     build: ./frontend
    #     volumes:
    #       - ./frontend:/project/srcDir:rw
    #     working_dir: /project
    #     restart: unless-stopped
    #     command: tail -f /dev/null
    # frontend:
    #     image: busybox
    #     volumes:
    #       - ./frontend/dist:/project
    #     working_dir: /project
    #     ports:
    #       - "9065:80"
    #     links:
    #       - backend
    #     restart: unless-stopped
    #     command: /bin/busybox httpd -f -v -p 0.0.0.0:80 -h /project
